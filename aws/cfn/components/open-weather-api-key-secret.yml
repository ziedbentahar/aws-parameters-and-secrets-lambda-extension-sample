AWSTemplateFormatVersion: "2010-09-09"
Description: open weather api key secret

Resources:  
  OpenWeatherApiKeySecret:
    Type: AWS::SecretsManager::Secret
    Properties: 
      Description: open weather api key secret
      Name: open-weather-api-key-secret

Outputs:
  OpenWeatherApiKeySecretArn:
    Description: OpenWeatherApiKeySecretArn An
    Value: !GetAtt OpenWeatherApiKeySecret.Arn
    Export:
      Name: !Sub ${AWS::StackName}-open-weather-api-key-secret
      